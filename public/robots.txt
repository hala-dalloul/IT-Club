# One group on purpose: a crawler obeys only the most specific group naming it,
# so a Googlebot-only group would silently skip every rule written under *.
User-agent: *
Allow: /
Disallow: /admin
Disallow: /club/admin
